name: dart7
description: Flutter приложение с чистой архитектурой, Dio, get_it и go_router
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Сеть
  dio: ^5.4.0
  pretty_dio_logger: ^1.3.1

  get_it: ^7.6.4

  go_router: ^13.0.0

  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

